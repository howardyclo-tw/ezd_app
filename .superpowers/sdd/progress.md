# MTK Features — SDD Progress Ledger

Plan: docs/superpowers/plans/2026-07-02-mtk-features.md
Branch: dev
Orchestrator: Fable | Implementers/Reviewers: Opus (per user)

## Tasks
- [x] 0.1 Install & configure Playwright
- [x] 0.2 Seed script for e2e fixtures
- [x] 0.3 Regression baseline specs
- [x] 0.4 Fix ESLint flat-config tooling (unblock lint gate)
- [x] 1.1 Migration 010 card_orders -> orders
- [x] 1.2 Migration 011 pricing/voting/penalty
- [x] 1.3 Refresh types/database.ts
- [x] 1.4 date + isMemberActive + pricing resolver
- [x] 1.5 Collapse isMember + remove dead code
- [x] 2.1 course_fee order lifecycle
- [x] 2.2 Fix hard-coded 1-card refund
- [x] 2.3 Review center 報名繳費 tab
- [x] 3.1 computeSessionOccupancy extract
- [x] 3.2 allocation module (fcfs)
- [x] 3.3 atomic enroll RPC
- [x] 3.4 waitlist cancel bug
- [x] 4.1 monthly purchase window
- [x] 4.2 n-multiple unit
- [x] 4.3 expiry sync + cascade
- [x] M1 繳費對帳 merge (堂卡+課程費→統一分頁)
- [x] 5.1 enroll gating + windows
- [x] 5.2 course form pricing/enroll fields
- [x] 5.3 enrollment wizard
- [x] 5.4 modify-as-rebook
- [x] 5.5 single add-enroll + no-cancel
- [x] 5R.1 identity-eligibility (per-course enroll_full/single_identity)
- [x] 5R.2 wizard entry + window states + admin quick-edit
- [x] 5R.3 modify-as-rebook UI (summary card + AlertDialog)
- [x] 5R.4 embedded purchase unit validation + prefill
- [x] 5R.5 personal center 我的堂卡・繳費
- [x] 5R.6 pending-status restrictions + roster badges + card-pool exhaustion
- [x] 5R.7 繳費對帳 group-by-檔期 + course form 單堂時窗預填
- [x] 5R.8 學員/幹部雙旅程走查 e2e + UI 設計修正
- [ ] 6.1 poll authoring + tally
- [ ] 6.2 vote-in-wizard integrity
- [ ] 6.3 publish + settle RPC
- [ ] 7.1 penalty computation + guard
- [ ] 7.2 blacklist admin tab
- [ ] 8.1 status surfacing + todos
- [ ] 8.2 full-suite gate + tracker
- [ ] 9.1 Lint cleanup: auto-fix + mechanical rules
- [ ] 9.2 Lint cleanup: no-explicit-any file-by-file
- [ ] 9.3 Lint cleanup: residual warnings + flip gate to whole-repo

## Log
Task 0.1: complete (commits f4b5fdd..7d8882c, review clean after .gitignore fix). Minors for final review: login-selector /^登入/ hardening; no webServer config.
Task 0.2: complete (commits 7d8882c..ca84a7b, review approved; 2 Important findings fixed: listUsers pagination, README prod-warning). Minors for final review: admin member_valid_until set w/o group (cosmetic); README 'published' wording; .env quote parsing.
NOTE (Phase 1): live 'courses' table has NO 'status' column though 001_course_schema.sql defines CourseStatus — Task 1.3 type refresh must drop/adjust Course.status. Adds to the 'live DB ahead of repo' set.
Task 0.3: complete (commits c18b951..ce1478d; review->fix->rereview->fix2->verified). 8/8 e2e green x2 runs. Strengthened: real past-session-leave, FIFO-expiry, attendance-persistence asserts; false-green guards removed; single-enroll coverage on dedicated 2nd course. tsc clean.
PHASE 0 COMPLETE (harness + seed + green regression baseline).
BLOCKER FOUND (pre-existing, not caused by our work): pnpm lint is broken. Next 16.1.1 removed 'next lint'; repo has legacy .eslintrc.json but ESLint 9 defaults to flat config and ignores it; no eslint.config.* present. Lint gate is non-functional until tooling migrated. tsc + build + e2e gates work. AWAITING user decision on lint-gate handling.
Task 0.4 ADDED (from user decision): migrate to ESLint flat config for Next 16; per-phase gate = lint CHANGED files only; report full-repo issue count for later triage; do NOT fix pre-existing repo lint debt.
Task 0.4: complete (commit 3314a1a). pnpm lint works via ESLint flat config. Repo debt: 318 errors/112 warnings pre-existing (no-explicit-any in actions.ts etc) — out of scope. Our files clean.
GATE POLICY (recorded in plan): per-phase lint = new files ESLint-clean; modified files add NO new violations. Do NOT gate on whole-repo pnpm lint. Gates that WORK: tsc --noEmit, pnpm build, e2e (happy+adversarial+regression).
PHASE 0 fully complete (0.1-0.4). Baseline: 8/8 regression e2e green. Next: Phase 1 Task 1.1 (card_orders->orders migration).
SCHEDULED (user request 2026-07-03): Phase 9 repo-wide lint-debt cleanup (430 problems). Runs LAST — after Phase 8, when full e2e safety net + typed database.ts exist. Correction: repo was Next16+ESLint9 from initial commit (2026-01-04); lint NEVER worked (broken next-lint script + ignored .eslintrc.json). Not an upgrade artifact. Recorded in plan Phase 9 + tracker.
Task 1.1: complete (commit 28e22b7 + migration-idempotency fix). Reviewer APPROVED: rename total, order_type='card_purchase' filter verified on ALL card reads incl FIFO deduct + balance sync, all INSERTs explicit. 8/8 regression green, tsc clean. 2 Important (migration SQL hygiene) fixed directly. Minors for final review: defense-in-depth order_type filter on status UPDATEs (confirmCardOrder/cancelCardOrder/rejectCardOrder/submitRemittanceInfo) — safe (PK + prior filtered SELECT).
Task 1.2: complete (commit 318e4a2). Reviewer APPROVED, no critical/important. RLS verified SELECT-only (users cannot forge poll_votes / penalty_overrides). Idempotent, additive, regression 8/8 green. Backfill: 1 style->ntd, 16 normal unchanged, 0 workshop on dev.
PROD-CUTOVER NOTE: 011 backfill reclassifies existing workshop+style courses to ntd (NULL prices). On prod, admins must set NTD prices for those before enrollment works (Phase 5 guard blocks NULL-priced ntd). Track in Task 8.2 prod checklist.
Task 1.3: complete (commit 55c4160). types/database.ts synced to live schema + contract. tsc clean (fully additive, no consumer changes), regression 8/8 green. Self-verified Order/Course/enums/new-interfaces match live schema + Shared Interface Contract. No full review needed (types-only, compiler-gated).
Task 1.4: complete (commit 903ce73). pricing resolver + isMemberActive + date helper, TDD 9/9 green, tsc clean, code matches brief verbatim. Design notes for downstream: (1) member w/o group => inactive; (2) card-full = per-session*count, caller prorates mid-term; (3) ntd price 0 => free (no order). No review workflow (tiny pure fn, test-gated).
Task 1.5: complete (commit d6623bd). isMemberActive unified into createCardOrder + import-actions; deleted getCardPriceForUser + enrollment-button. Regression 8/8 green (270/370 preserved), tsc clean.
INTENDED behavior change (record for prod/stakeholders): bulk card_orders import now prices EXPIRED members as non-member (370) unless CSV 'amount' overrides — was role-only before. Consistent with app-wide isMemberActive. Import path has no e2e coverage.
Minor for Phase 9: one new 'as any' on member_groups join in import-actions.ts (matches pervasive codebase pattern).
*** PHASE 1 (FOUNDATION) COMPLETE: orders table, pricing_mode/enroll cols, voting/penalty tables, synced types, pricing resolver, unified isMemberActive. ***
SEQUENCING (recorded in plan): Task 2.1 e2e deferred — course_fee confirm/cancel e2e -> Task 2.3 (review-center UI); create-from-enroll e2e -> Phase 5. 2.1 gate = tsc + regression (card path preserved).
Task 2.1: complete (be1c6c1 + fix 1e8b17e). Unified order lifecycle (confirmOrder/cancelOrder/rejectOrder branch on order_type; card wrappers preserved). Reviewer APPROVED after 3 Important fixes: confirm/reject terminal-status guards, createCourseFeeOrder made internal (no unauth endpoint), cancelOrder idempotency+error-check. Regression 8/8, tsc clean. Self-verified guard placement. course_fee e2e -> Task 2.3.
BACKFILL REVIEWS (no-self-review directive): 1.3 APPROVED clean. 1.4 APPROVED (impl correct) — adding 3 missing edge tests (boundary date, ntd-null throw, fallback) + 3 minor per 100%-coverage. 1.5 APPROVED — DOCUMENT behavioral delta: createCardOrder now prices a member with NO group AND no member_valid_until as non-member (370); was member before. Data-error edge; flag to client. 2.1-fix NEEDS_FIXES: rejectOrder course_fee branch missing enrollment-update error check (seat-leak on failed reject); reject msg points to cancel which also blocks confirmed (dead-end) — fix msg.
Task 2.2: impl DONE (1d02173) — reviewSingleEnrollment refunds/re-deducts cards_per_session (was 1); new e2e/features/refund-count.spec.ts green (reject 2-card course -> +2); regression 8/8; tsc clean. PENDING INDEPENDENT REVIEW.
ROUND-2 REVIEWS (all APPROVED, no critical/important): 2.2-refund clean (FIFO refund loop = exactly cards_per_session, order_type filter present, e2e non-vacuous +2); 1.4-tests clean (7 new tests non-vacuous, edges covered, 16/16); 2.1-reject-fix clean (error-check present, message fixed). 
Task 2.2: COMPLETE (1d02173, reviewed). Phase 1 (1.1-1.5) now FULLY independently reviewed (self-review gap closed).
MINORS logged for final whole-branch review: refund under-refund if pools used<cardsPerSession (pre-existing); e2e balance locator uses .text-7xl/.text-8xl (brittle); pricing toThrow lacks message match; enrollError.message returned (not throw) so safe() won't redact in prod (pre-existing pattern in cancelOrder+rejectOrder); createCardOrder redundant getTaipeiToday; scattered as-any (Phase 9).
Task 2.3: COMPLETE (f206dad, reviewed APPROVED). Review-center 報名繳費 tab + FIXED prior vuln: confirmOrder/rejectOrder had ZERO role check (any auth user could approve/reject any order incl card orders) -> now admin-only; cancelOrder admin-or-owner; fail-closed. Adversarial e2e genuine (member confirm rejected, guest cancel-others rejected). Regression 12/12. Bonus: adminClient switch fixed latent cross-user RLS bug on card confirm.
*** PHASE 2 COMPLETE (2.1 order lifecycle, 2.2 refund-count, 2.3 review-center+authz). ***
PROD-CHECKLIST (Task 8.2) additions: (a) remove/relocate src/app/api/e2e-test-actions/route.ts before prod (local-only now via NODE_ENV, but shouldn't ship); (b) consider migration to relax card_orders_quantity_check for non-card_purchase order types (course_fee uses quantity=1 workaround); (c) align approvals page cardOrderQuery to adminDb like courseFeeOrderQuery (pre-existing RLS inconsistency).
NEXT: pre-Phase-3 regression backfill for makeup + transfer (per no-self-review/100%-coverage directive; Phase 3 rewires enrollment engine). waitlist-cancel coverage folds into Task 3.4 (fixes the cancel-button bug).
PRE-PHASE-3 BACKFILL COMPLETE (commits 59b4671 -> 2e5ed43, reviewed->fixed->re-reviewed APPROVED): makeup + transfer regression (happy + adversarial) with DB-state assertions (e2e/fixtures/db.ts). Review caught a real FALSE-GREEN (past-session leave test frozen via seeded absence not isPast) — FIXED with a separate un-marked past session; verified it now fails if isPast guard removed. Regression now 12 cases green.
MINORS/notes: (a) enroll-single.spec.ts intermittently flaky (30s nav timeout) — pre-existing, stabilize if it recurs in gates; (b) db.ts getUserIdByEmail + seed-users.mjs use unpaginated listUsers (latent if dev users >50); (c) normal/special transfer shared-quota branch not e2e-covered (workshop-only happy path, documented gap). All logged for final whole-branch review.
*** READY FOR PHASE 3 (capacity module, allocation, atomic enroll RPC, waitlist-cancel bug). Backfill safety net in place. ***
NAMING/UX DECISION (user): MERGE the two payment-reconciliation tabs (堂卡訂單 + 報名繳費) into ONE '繳費對帳' tab — reflects the unified orders table (order_type card_purchase|course_fee, shared lifecycle/actions). Each row: type badge (堂卡/課程費) + type-appropriate fields; filter chips 全部/堂卡/課程費. Resolves 報名繳費↔單堂報名 naming collision (tab bar 6→5). Follow-up task refactors Task 2.3 output + its e2e. Convention: money/對帳 tab = 繳費對帳; enrollment reviews keep record names (單堂報名/請假/補課/轉讓). Audit upcoming feature names (整期報名 wizard, MV投票, 班長意願, 黑名單) for collisions before each ships.
Task 3.1: COMPLETE (2ab9ecb, reviewed APPROVED). computeSessionOccupancy extracted (src/lib/supabase/capacity.ts), 13 non-vacuous unit tests, 3 call sites unified, counts pending_* as active (no-op today). Minor: course-detail now subtracts approved future leaves (more accurate); O(n*s) vs maps (negligible). No fixes needed.
STABILIZE (8634f94, reviewed APPROVED-with-issues): globalSetup re-seeds per run (deletions properly scoped to e2e users+UUIDs, prod-guarded), networkidle->element waits, 12/12 x2. 2 IMPORTANT to fix: (1) seed drift (seed.sql vs global-setup.ts duplicate) -> make globalSetup single source, retire seed.sql; (2) addDays off-by-one (UTC vs Taipei) -> all seed session dates 1 day early (latent). Fixing next.
*** PHASE 3 progress: 3.1 done. Gate now deterministic (auto-reseed). Next: fix stabilize -> 繳費對帳 merge -> 3.2 allocation -> 3.3 atomic RPC -> 3.4 waitlist. ***
STABILIZE FIX COMPLETE (6373c17, re-reviewed APPROVED all_resolved): addDays -> Taipei date (verified); seed.sql DELETED, globalSetup is single seed source (all 12 entities cross-checked), refs updated. Gate now deterministic + single-source + date-correct. Regression 12/12.
NEW TASK M1 (繳費對帳 merge, user UX decision): merge review-center tabs 堂卡訂單 + 報名繳費 -> ONE 繳費對帳 tab (type badge 堂卡/課程費 + 全部/堂卡/課程費 filter). Refactors Task 2.3 output + MUST update card-purchase regression tab navigation + review-center-coursefee feature spec. Dispatching.
Task M1 (繳費對帳 merge): COMPLETE (db46db7, reviewed APPROVED, zero findings). Per-row order_type branching, 全部/堂卡/課程費 filter, both specs strengthened. Tab bar 6->5. All e2e green.
Task 3.2 (allocation fcfs): COMPLETE (6efa497, reviewed APPROVED). Pure module, 7 non-vacuous tests, exhaustive-switch extensible. Minor: localeCompare locale (cosmetic, safe for ISO/UUID).
Task 3.4 (waitlist-cancel): COMPLETE (8e7dc44, reviewed APPROVED). 取消候補 rebound to handleCancelEnrollment; cancelEnrollment verified to cancel waitlist w/o promotion; non-vacuous e2e (DB assert). Minors: unused var, waitForTimeout(1000) (cosmetic).
EFFICIENCY PROFILE working: 3.2=25k tok/2.2min (medium+unit-only), reviews batched in parallel. vs earlier 60-190k/5-25min.
IN FLIGHT: flakiness diagnosis+fix (enroll-single balance flake — gate not reliably green; likely inter-test balance coupling since globalSetup reseeds once/run). BLOCKS 3.3 (atomic RPC) until gate is reliably green x3.
FLAKE FIX (186f669, DONE_WITH_CONCERNS): root cause = stale balance read after enroll action (fixed waitForTimeout raced server action + revalidatePath). Fixed enroll-single + card-purchase with toast-wait + expect.poll delta (order-independent, not weakened). Target tests 3/3 green. BUT run3 surfaced a DIFFERENT flake: attendance-leave.spec.ts:97 (E2E Member not visible, SSR streaming race). Gate not yet reliably 3x-green.
NEXT: one systematic hardening pass — fix attendance-leave:97 + audit ALL specs for post-navigation race patterns (assert-before-content) -> deterministic waits; prove full suite 13/13 green x3. Then review combined stabilization, then 3.3.
HARDENING (ff8c1f3): 39 race sites hardened across 9 specs (deterministic waits, no assertion weakening claimed). Full regression 13/13 green x3 consecutive + features 4/4. tsc clean, no app source. Gate now RELIABLE (pending assertion-integrity review). Remaining 4-5 short waitForTimeout guard client-side React state (not races).
STABILIZATION COMPLETE + REVIEWED APPROVED (186f669+ff8c1f3): assertion integrity verified across 39 sites + 2 balance fixes (exact deltas via expect.poll, no weakening). Gate RELIABLE: 13/13 x3 + features 4/4. Minor logged: card-purchase.spec.ts:68 no-op save-wait (not a false-green, setup-only). *** GATE TRUSTWORTHY -> proceeding to 3.3. ***
Task 3.3 RPC review: APPROVED (code). Occupancy SQL = faithful computeSessionOccupancy port; FOR UPDATE serializes (oversell prevented, test passes); FIFO = card-utils parity; exception rollback correct; +bonus batchEnrollInCourses capacity check. 1 IMPORTANT to fix: insufficient_cards path doesn't report partial enrollment / revalidatePath (mirror the full-capacity handler) — in batchEnrollInSessions + batchEnrollInCourses. Minors: 013/012 numbering gap; generic card_tx note. Fix pending until auth-flakiness fix (A) stops committing.
AUTH-FLAKE FIX (77cc4f6, reviewed APPROVED): root cause = React19/Next16 event-delegation wired AFTER fiber hydration signal -> submit-in-gap does native GET -> stuck /login. Fixed via storageState reuse (login once/role in globalSetup w/ retry, restore cookies in tests). loginAs verifies via /dashboard URL assert (middleware redirect = loud fail). enroll-single green x3 => 3.3 enroll path CONFIRMED safe empirically. Suite 13min->3min. Security: .auth gitignored, no tokens committed. Minors (final review): ACCOUNTS dup, base URL dup, localStorage-clear asymmetry.
3.3-fix (912dc02): insufficient_cards path now reports partial enrollment + revalidatePath in both batch fns. tsc clean, enroll-single green. Pending quick review.
Task 3.3 (atomic enroll RPC): COMPLETE (bbf3114 + fix 912dc02, both reviewed APPROVED). RPC verified (occupancy parity, FOR UPDATE serialization, FIFO parity, exception rollback); oversell concurrency test passes; partial-enroll reporting fixed. enroll-single green x3 (empirically safe).
*** PHASE 3 COMPLETE (3.1 occupancy, 3.2 allocation fcfs, 3.3 atomic RPC anti-oversell, 3.4 waitlist-cancel). Gate reliable+fast (storageState, ~3min, 13/13 x3). Running independent phase-3 full-suite gate @912dc02 as backstop. ***
Task 4.1 (monthly purchase window): DONE (740c59a, pending batch-review). getCardPurchaseWindow first-Monday offset (24 unit tests, all weekday-starts+Feb+rollover); wired default=manual (regression green); monthly mode opt-in; adversarial out-of-window test passes. Controller sanity-checked offset math = correct. tsc clean.
Task 4.2 (n-multiple unit): DONE (53cbdf8, pending batch-review). Server guard quantity>0 && %unit==0 (config default 5); stepper reads unit; adversarial 4/4 + regression green. FLAG for review: confirm adversarial hits SERVER guard directly (bypass-UI), not just UI stepper.
Task 4.1 (purchase window): COMPLETE (740c59a, reviewed APPROVED). First-Monday math verified all 7 weekday-starts; 24 tests; default manual preserves regression; server guard + UI agree. Minors logged: window adversarial e2e checks UI only; card_purchase_mode free-text (falls through to manual safely).
Task 4.2 (n-multiple): NEEDS_FIXES — guard code correct BUT adversarial test is FALSE-GREEN (qty=7 queried twice, no mutation, tautology) + server guard unverified; config footgun (NaN/0 unit rejects all). FIX: extract pure validatePurchaseQuantity helper (unit-test non-multiple/0/neg/NaN-unit->default 5), wire into createCardOrder+UI, replace tautological test. Log (no fix): upper-bound cap = product decision; mode dropdown = polish.
Task 4.2-fix (15d916b): pure validatePurchaseQuantity+sanitizePurchaseUnit (22 unit tests, real coverage), wired into createCardOrder+UI, false-green tests deleted+replaced honest. 86 unit pass, regression green, tsc clean. Pending quick review. Concern logged: no upper-bound cap (product decision).
Task 4.2 (n-multiple): COMPLETE (53cbdf8 + fix 15d916b, reviewed APPROVED resolved=yes). validatePurchaseQuantity wired into createCardOrder (verified), 22 unit tests, false-green removed, NaN/0 fallback. Phase 4: 4.1 ✓, 4.2 ✓, 4.3 (expiry cascade) in flight.
Task 4.3 (expiry sync+cascade): DONE (a9b2f6c, pending review). createCardOrder expiry=buyer own group (fallback year-end); updateMemberGroup cascade updates matching confirmed orders + syncBalance; card_expire_month dropped from UI. Cascade e2e (2) + regression green, tsc clean.
Task 4.3 review: APPROVED (code correct — cascade scoping precise: 4 WHERE conds protect custom-expiry/other-group/non-confirmed/non-card orders; expiry from buyer own group; balance integrity holds). 2 IMPORTANT test-gaps to close: (1) e2e replicates cascade logic instead of driving REAL updateMemberGroup action; (2) NO negative-case (custom-expiry order asserted untouched). Minor real bug: guest fallback expiry uses UTC getFullYear not Taipei. Fixing test-strength + tz.
Task 4.3-fix (e9e15fb): cascade e2e drives REAL updateMemberGroup (via test route) + negative-case (custom-expiry order untouched); guest tz-year -> Taipei; silent cascade error now throws. tsc clean, cascade 2/2 + card-purchase green. NOTE: seed member card_balance 10->15 + new custom-expiry order -> globalSetup baseline change affecting ALL specs -> Phase-4 full gate must validate. Pending fix-review.
Task 4.3 (expiry cascade): COMPLETE (a9b2f6c + fix e9e15fb, reviewed APPROVED resolved=yes). Real-action e2e + negative-case (custom-expiry untouched); updateMemberGroup admin-gated; tz + silent-error fixed; seed change safe (delta-based specs). Minor logged: stale card_balance=10 comment in refund-count.spec.ts:11.
*** PHASE 4 COMPLETE (4.1 monthly window, 4.2 n-multiple, 4.3 expiry cascade). Running independent Phase-4 full-suite gate @e9e15fb (validates seed baseline change across all specs). ***
*** PHASE 4 GATE GREEN @e9e15fb: tsc clean, 86 unit, 23/23 e2e (2.9m). Seed baseline change validated across all specs. PHASE 4 COMPLETE. ***
Task 5.1: BLOCKED — implementer subagent died on org MONTHLY SPEND LIMIT (not a code issue). Partial unverified edits (actions.ts, global-setup.ts, test route) DISCARDED; repo clean at e9e15fb (last gate-verified). Cannot dispatch Opus subagents until limit raised (/usage-credits) or month resets. Resume: re-dispatch 5.1 fresh.
Task 5.1 (enroll gating): COMPLETE (472539b, independent Opus-4.6 review APPROVED zero-findings + MUTATION-TESTED: removing enroll_single guard and course-window guard each turned the matching adversarial test RED -> confirmed no false-green). Added 5 shared guard helpers (guardPricingMode/guardEnrollFull/guardEnrollSingle/guardGroupPhase1Window/guardCourseWindow) applied to ALL 3 enroll entry points (enrollInCourse[legacy, ZERO prod callers-hardened for defense], batchEnrollInCourses[full], batchEnrollInSessions[single, previously had NO window/mode/pricing guard - main hole closed]). full=group phase1 window; single=course enrollment_start/end_at; timestamptz compared as INSTANTS (no Taipei date-string conversion). e2e-test-actions route extended (NODE_ENV-gated) with 3 enroll actions. 4 isolated seed fixtures (new UUIDs, no baseline mutation). New enroll-gating.spec.ts (2 happy + 4 adversarial, all drive real action + assert no-DB-row). ORCHESTRATOR-VERIFIED full suite alone: 29/29 green (4.6m) - the impl(5-fail)/review(2-fail) discrepancies were CONTENTION artifacts (both stages ran full single-worker Playwright vs one dev server -> timeouts + 17min runtime), NOT regressions. tsc clean, 86 unit, no new lint. Minor (reviewer, non-blocking): no dedicated enroll_full=false adversarial test on batchEnrollInCourses (same trivial shared helper as covered single path).
WORKFLOW-DESIGN LESSON: do NOT have BOTH implementer and reviewer stages run the full e2e suite - single-worker Playwright + one shared dev server = contention -> false-timeout failures + 3-6x slowdown. Fix forward: implementer runs ONLY the new/scoped spec; reviewer runs the full suite (it's the gate); orchestrator does a final clean single-run confirm.
Task 5.2 (course form pricing): COMPLETE (cbad7e5, independent Opus-4.6 review APPROVED zero-findings + MUTATION-TESTED: removing superRefine -> blank-price validation test went RED). Added pricing_mode + 4 NTD prices (member/guest x single/full) + enroll_full/enroll_single to zod schema, conditional required-price validation (ntd only, per-enabled-mode, 0 valid via optionalPrice preprocess distinguishing blank->null from 0), conditional rendering (card->cards_per_session, ntd->prices, free->neither), type-default prefill map (normal/trial/special=card+both; workshop=ntd+both; style=ntd+single-only+member-price-0) guarded by isEdit to not clobber. Both createCourse+updateCourse persist all 7 columns w/ null-vs-number normalization + server ntd-null-price defense throw. New course-form-pricing.spec.ts (3 tests: create-ntd, blank-price-blocks, style-defaults) w/ afterAll cleanup. CORRECTED WORKFLOW DESIGN worked: impl ran only new spec, reviewer ran full suite ALONE = 32/32 green 3.9m (no contention, vs 5.1's false 5/2-fail). Orchestrator light-confirm: tree clean, tsc clean, new spec 3/3. Lint: +6 no-explicit-any in course-form (same form.control as any pattern as 20+ existing FardFields), actions.ts zero new. Reviewer minor non-blocking gaps: no updateCourse-pricing e2e (logic identical), no free-mode e2e (same null path as card), no edit-load e2e (isEdit guard correct by inspection).
*** PHASE 5: 5.1 ✓ 5.2 ✓. Next 5.3 (register wizard + submitGroupEnrollment) — SERIALIZE (overlaps actions.ts enroll paths). ***
Task 5.3 (register wizard + submitGroupEnrollment): IMPLEMENTED (dbd5064, independent Opus-4.6 review APPROVED but with 2 MINOR money-coverage gaps + mutation-tested: neutralizing guardEnrollFull AND swapping resolvePrice for client amount each turned matching adversarial test RED). Full suite 37/37 green (5.1m). submitGroupEnrollment routes MV->pending_vote(no deduct/order), free->enrolled, card-afford->enrolled+deduct, card-shortfall+buyCards->pending_payment+card_purchase order, ntd->pending_payment+course_fee order(server-summed amount); all inserts via enroll_atomic (atomic capacity); client price IGNORED (server resolvePrice); one-fails-others-succeed; wants_leader persisted; votes deferred to 6.2 (TODO hook); reused guardEnrollFull+guardGroupPhase1Window+guest guard. 7 isolated fixtures in regGroup(...012). 5 tests (happy mixed, adv enroll_full-reject, adv client-price-ignored, adv capacity-full, UI smoke).
ORCHESTRATOR OVERRIDE (per user 100%-important-coverage rule > reviewer MINOR severity): 5.3 NOT accepted as done. Two MONEY paths uncovered: (1) card-shortfall+buyCards (creates card_purchase order+link) — AND fixture regCardShortfall(b2) is misconfigured: 3 cards/session x 2 sessions = 6 < 15 balance = affordable, NOT a shortfall; comment says 24. (2) card-shortfall-no-buyCards (reject 堂卡不足). Dispatching 5.3-fix: correct fixture so full-term cost > seeded balance(15), add both tests, independent reviewer mutation-tests the shortfall+order path.
Task 5.3-fix (shortfall coverage): COMPLETE (3b67f3e, independent Opus-4.6 review APPROVED zero-findings + BOTH mutation tests RED). CAUGHT A REAL BUG: shortfall+buyCards path called user-facing createCardOrder() which enforces purchase-window/min/unit checks (wrong during group enroll) AND silently ignored build-failure (orphan unlinked pending_payment). Fixed -> direct adminClient card_purchase insert + throw-on-failure (mirrors createCourseFeeOrder). Fixture regCardShortfall now 3 cards/session x 6 sessions = 18 > 15 balance (genuine shortfall). +2 money tests (T1 pending_payment+card_purchase order linked+balance-unchanged; T2 reject 堂卡不足+no row). register-wizard 7/7. Full suite 39/39 (4.9m), 86 unit, tsc clean. VINDICATES the override (requiring money coverage surfaced a latent prod bug).
*** PHASE 5: 5.1 ✓ 5.2 ✓ 5.3 ✓(incl fix). Next 5.4 (作廢重報/resubmit). ***
5.4 building blocks: card refund pattern = restore orders.used earliest-expiry-first + syncCardBalance (see reviewSingleEnrollment ~3097-3118); idempotency via conditional status update WHERE status='enrolled' (see ~3082); enroll_atomic RPC = SQL card-logic precedent (migration 013). submitGroupEnrollment exists. resubmit not yet.
Task 5.4 (modify-as-rebook / resubmitGroupEnrollment): COMPLETE (17ff903, independent Opus-4.6 review APPROVED zero-findings + BOTH mutation tests RED). Postgres RPC void_group_submission (migration 014, applied dev + repo file): single-txn atomic void (cancel enrollments + refund cards restore-used + cancel pending/remitted orders), FOR UPDATE + conditional WHERE status IN(active) = idempotent (2nd resubmit sees cancelled -> refunds 0), confirmed-order EXISTS check refuses before any mutation, balance recomputed from scratch (no drift). JS resubmitGroupEnrollment = void RPC then re-run submitGroupEnrollment (reuses all guards, later enrolled_at). e2e-test-actions extended. 3 tests (happy void+refund+re-enroll later-ts; adv confirmed-order-blocks-intact; adv repeated-resubmit-conserves 15->13 no-dup). Full suite 42/42 (5.3m), 86 unit, tsc clean. Mutation: remove confirmed-refusal->adv-a red; break idempotency->adv-b double-refund red.
5.5 building blocks: batchEnrollInSessions (actions.ts:444) currently guardPricingMode-REJECTS non-card (465) -> 5.5 REPLACES with routing (card=deduct existing / ntd=course_fee order+pending_payment / free=immediate enrolled, blacklist=Phase7 TODO). Keep guardEnrollSingle(468)+guardCourseWindow(471). cancelEnrollment (579) cancels any non-cancelled -> HARDEN: self-cancel only if waitlist OR pending_payment(unconfirmed); enrolled BLOCKED. Preserve waitlist-cancel regression + waitlist promotion for freed occupying seat. UI: session-enrollment-dialog.tsx + course-detail-client.tsx.
Task 5.5 (single add-enroll pricing + no-self-cancel): COMPLETE (605ba04, independent Opus-4.6 review APPROVED + BOTH mutation tests RED). batchEnrollInSessions now pricing-routes: card=deduct(verbatim, regression-preserved) / ntd(price>0)=pending_payment+course_fee order(server price) / ntd(0)+free=enrolled no-deduct no-order; guardPricingMode removed here, guardEnrollSingle+guardCourseWindow kept; client price structurally unreachable (no amount param). cancelEnrollment HARDENED: enrolled=REJECT(all modes), pending_payment+confirmed=REJECT, pending_payment-unconfirmed + waitlist=ALLOW(+promote freed occupying seat). 5 tests (ntd happy, free happy, enrolled-no-self-cancel, guest-free-no-self-cancel, ntd client-price-ignored) + updated enroll-gating adv(d). waitlist-cancel + enroll-single regressions GREEN. tsc clean, 86 unit. Reviewer MINOR (non-blocking, logged): unused guestId var in single-add-enroll.spec.ts (lint warning). Reviewer full suite 46/47 — 1 fail = PRE-EXISTING waitlist-cancel flake (getByText 'E2E Single Course' strict-mode 2-match during SSR nav race), passes on re-run.
*** PHASE 5 COMPLETE (5.1 gating, 5.2 course form pricing, 5.3 wizard+submitGroupEnrollment, 5.3-fix money coverage, 5.4 atomic rebook, 5.5 single-pricing+no-self-cancel). Running orchestrator full-suite gate backstop next. ***
FLAKE TO HARDEN (phase-5 gate finding): waitlist-cancel.spec.ts getByText('E2E Single Course') is a STRICT-MODE 2-match (not just timeout) during SSR nav — needs a more specific locator. Log for hardening.
=== PHASE 5 USER ACCEPTANCE (2026-07-05, Fable-5 max direct) ===
ALIGNMENT AUDIT vs decision-log/spec: MISALIGNED (a) #5 guest-full open per log but code blanket-blocks (batchEnrollInCourses~300 + submitGroupEnrollment~3375) -> superseded by NEW DECISION #32 per-course enroll_full_identity/enroll_single_identity (all/member, default all, isMemberActive standard) [5R.1]; (b) spec-§7 embedded-purchase unit validation lost in 5.3-fix bypass [5R.4]. SPEC-WRITTEN-BUT-UNBUILT: /register entry+window button states [5R.2], modify-rebook UI [5R.3], personal payment center + 稍後補匯款 (#31 pulled forward) [5R.5], pending-status restrictions + roster badges incl wants_leader badge [5R.6], 繳費對帳 group-by-檔期+待審 [5R.7], normal single-window 開課後 prefill [5R.7]. VERIFIED-OK: direct card-order insert fields complete (unit_price 270/370, +1800, expires_at=buyer group) — false alarm. Admin window-setting EXISTS but buried (course-form group edit) -> discoverability fix 5R.2.
USER DECISIONS: identity=per-course setting (more general than #5); group-unified pricing = type defaults suffice; tracker RETIRED -> matrix merged into dashboard (decision #25 v4). NEW #33 UX-in-process rule (dual-role spec + walkthrough e2e + entry-point rule = gate conditions).
ARTIFACTS: specs/2026-07-05-mtk-ux-addendum.md (5R source of truth + P6-P8 dual-role UX prewritten); plan += Phase 5R (8 tasks, count 35->43) + Global Constraints (UX rules/contention/doc-sync); decision-log v4 (#32/#33, #5/#25 amended); dashboard 25/43 + matrix merged + P5R block, Artifact redeployed; tracker removed (spec §15 ref updated); CLAUDE.md REWRITTEN as binding agent rules; NEW docs/superpowers/mtk-execution-playbook.md; NEW .claude/settings.json + hooks/block-git-push.py (verified live: blocked 2 false-positives during setup -> regex anchored to subcommand + unit-tested 12 cases). Memory project_mtk_features updated.
Task 5R.1 (identity-eligibility): COMPLETE (d1d7924, independent Opus-4.6 review APPROVED + MUTATION-TESTED: disabling guardEnrollFullIdentity -> A1 RED "堂卡不足" instead of guard msg; disabling guardEnrollSingleIdentity -> A2 RED "堂卡餘額不足" -> confirmed both guards are real, no false-green). Migration 015 (enroll_full_identity/enroll_single_identity TEXT NOT NULL DEFAULT 'all' CHECK IN ('all','member')) applied to dev. REMOVED two blanket guest-full throws (batchEnrollInCourses~300, submitGroupEnrollment~3375). NEW guard helpers guardEnrollFullIdentity/guardEnrollSingleIdentity using isMemberActive (not raw role). Wired into ALL 3 enroll paths: batchEnrollInCourses (per-course filter), batchEnrollInSessions (after guardEnrollSingle), submitGroupEnrollment (per-course loop). createCourse/updateCourse persist new columns + NTD guest price only required when identity!='member'. Course form: zod + Select UI + conditional guest price fields. Register wizard: identityLocked flag + disabled reason. 5 e2e tests (H1 guest-card-success, H2 guest-ntd-price-1200, A1 member-only-full-reject, A2 member-only-single-reject, A3 ntd-null-guest-price-reject). Full suite 52/52 green (5.9m), 86 unit, tsc clean. UUID collision note: planned 0000000000e1-e5 collided with Phase 5.4 resubmit fixtures -> used 0000000001a1-1a5/1b1-1b9. Reviewer MINORs (non-blocking): wizard UI msg slightly more general than server msg (acceptable in context); 2 any in test file (project convention).
ORCHESTRATOR POST-REVIEW FIX (9d52b5b, amend): adversarial audit found `enrollInCourse` (legacy, line 123, 0 prod callers but exposed via e2e-test-actions route) was MISSING identity guard. Added guardEnrollFullIdentity+guardEnrollSingleIdentity to both full and single paths + expanded profile query for isMemberActive. ALL 4 user-facing enrollment entry points now covered: enrollInCourse, batchEnrollInCourses, batchEnrollInSessions, submitGroupEnrollment (resubmitGroupEnrollment covered transitively). import-actions admin bulk import intentionally unguarded (admin privilege). MINOR GAP NOTED: course-detail-client.tsx single enrollment UI does not preemptively show identity-locked state (server guard blocks, but UI doesn't warn before attempt) — supplement in 5R.2 or later task. Dashboard updated + Artifact redeployed.
PROCESS IMPROVEMENT: future tasks use orchestrator-implements + single reviewer agent (cuts ~50% cost vs two-agent Workflow). Orchestrator also runs new spec independently + scans diff for gaps post-review.
Task 5R.2 (wizard entry + window states): COMPLETE (791bbc2 + fix e92422d, independent Opus-4.6 review APPROVED). GroupEnrollmentDialog retired (247 lines deleted). Window state machine (not_configured/not_started/open/closed) on group page: admin sees date range + RegistrationWindowEdit pencil-icon quick-edit; student CTA lifecycle (no-button→disabled-with-date→Link→disabled-截止+single-hint). Register page: 3 friendly closed states (CalendarOff + message + back-link). hasExistingEnrollment expanded to enrolled/pending_payment/pending_vote. Fixtures: noWindowGroup(…015), futureWindowGroup(…016) + explicit slugs (e2e-main, e2e-closed-p1, e2e-nowin, e2e-futwin, e2e-resub, e2e-identity). 7 e2e tests (H1-H4 + A1-A3). Fix: DRY formatTaipeiDateTime to src/lib/date.ts; A3 test added; all fixture groups slugged; globalSetup login retry wraps entire attempt in try-catch. Full suite 46/46 green (4.8m), tsc clean.
NEXT: Task 5R.3 (modify-as-rebook UI).
	Task 5R.3 (modify-as-rebook UI): COMPLETE (d0f6fe5). Summary card with per-course status chips (constants badges), 修改報名 → AlertDialog strong warning (作廢/丟順位/額滿/堂卡退回/已確認付款擋), confirmed-order server refusal (resubmitGroupEnrollment). register-modify.spec.ts 2 tests (happy rebook + confirmed-order blocked). ENROLLMENT_STATUS_COLORS/LABELS added to constants.ts. tsc clean.
	Task 5R.4 (embedded purchase unit validation): COMPLETE (6e8406b). Server fail-fast in submitGroupEnrollment: validatePurchaseQuantity(buyCards.quantity, rawUnit) reuses shared validator. UI prefill = ceil(shortfall/unit)*unit, stepper step=unit min=prefill, submit disabled if < prefill. register-buycards-unit.spec.ts 1 adversarial test (non-multiple rejected). tsc clean.
	Task 5R.5 (personal center 我的堂卡・繳費): COMPLETE (99224f3 + fix 83a3626). my_cards dual tabs 堂卡|繳費紀錄 with ORDER_STATUS_COLORS/LABELS from constants.ts; row actions: pending/remitted→補匯款(submitRemittanceInfo)+取消(cancelOrder cascading enrollment cancel+seat release); confirmed/rejected/cancelled read-only. my_courses enrollment status badges (ENROLLMENT_STATUS_COLORS). Dashboard todo chips (待繳費/待開票). my-payments.spec.ts 5 tests. Fix: error toast on cancel failure, badge constant import, cancelReason destructure. tsc clean.
	REVIEWER FINDING on 5R.5: UI 設計不一致 — bg-orange-600 按鈕(應用 bg-primary)、balance card 過度裝飾(shadow-2xl/blur/rounded-[2.5rem])、max-w-lg 容器(應 max-w-5xl)、local STATUS_MAP 而非 constants import。列入 5R.8 修復。
	Task 5R.6 (pending-status restrictions + roster badges + card-pool exhaustion): COMPLETE (6c67221 + test-fix 4c2e9e5). Server guards: pending_payment/pending_vote enrollments BLOCKED from leave/transfer/makeup-source. Roster: pending badges (待繳費/待開票) + wants_leader badge (志願者). Card-pool exhaustion: confirmOrder when cards already used → stays remitted + clear error. pending-restrictions.spec.ts 4 tests (H1 happy+remittance, H1b remittance correction, A1 pending-leave-blocked, A2 pending-transfer-blocked). Fixture: cardExhaustCourse + 2 sessions. tsc clean.
	Task 5R.7 (繳費對帳 group-by-檔期 + course form 單堂時窗): COMPLETE (9489928). Approvals 繳費對帳 tab groups orders by course_group; phase1-closed groups with pending/remitted → 待審 chip. Course form: type=normal → enrollment_start_at prefills to first session date. review-center-coursefee.spec.ts updated (3 tests). tsc clean.
	WORKFLOW IMPROVEMENT (user feedback 2026-07-06): playbook updated with (1) task tiering (金流/混合/純UI/測試 → differentiated review depth), (2) UI design brief requirements (token/component/layout constraints in every brief), (3) progress sync enforcement (PreToolUse hook warns on commit without ledger update), (4) visual review replaces mutation-test for pure-UI tasks.
	Task 5R.8 (雙旅程走查 + UI 修正): COMPLETE (9720197). Adversarial verification of all 5R tasks against UX addendum spec found 3 FAILs + multiple WARNs. Fixed: (1) cancelled enrollments invisible in my_courses → show representative session with cancel_reason; (2) SessionCard cancel_reason rendered; (3) course-form 「單堂」missing from description. UI design sweep: all bg-orange-600→bg-primary, overstyled balance card→shadow-md rounded-xl, max-w-lg→max-w-5xl, dark: prefix patterns→opacity-based, roster hardcoded badges→ENROLLMENT_STATUS_COLORS constants. Student journey (S0-S4): 5 serial tests (pending_payment visibility, submit remittance, correct remittance, cancel order+DB assert). Admin journey (J1-J3): 3 serial tests (grouped 繳費對帳, confirm→enrolled, roster visibility). 8 journey e2e total.
*** PHASE 5R COMPLETE (5R.1-5R.8, 8 tasks). GATE: tsc clean, 86 unit, 79/79 e2e (13 regression + 58 features + 8 journeys). Dashboard updated + Artifact redeployed. ***
*** PHASE 5R SUMMARY: UX 補完 8 項任務。新增 identity-eligibility(per-course)、精靈入口+時窗狀態機、修改報名 UI、購卡預填、個人中心(堂卡+繳費)、pending 限制+名冊徽章、對帳分組+時窗預填、雙旅程走查。e2e 從 52→79(+27)。流程改善:任務分級+UI 設計約束+進度同步強制。 ***
	5R-HOTFIX (flow/UI gaps + dispatch model): 6 files, 173+/119-. FUNCTIONAL: bankInfo section rendered in payments tab (was passed but never displayed), tab deep-link via useSearchParams+Suspense (wizard done→payments, dashboard→payments), remittance info retained for confirmed/rejected (was remitted-only), rejected order guidance text, dashboard pending deep-link. REGISTER WIZARD: NTD→現金 badge, MV badge removed, free-course fee summary ($0免費), done page guidance+個人中心 button, container centering (mx-auto), isMember-aware NTD price display. CSS/UI (Gemini Flash via /agy): text-[10px/11px]→text-xs, dark: variants on all hardcoded colors, bg-primary/[0.03]→bg-primary/10, cancelled order opacity-60, balance gradient text-transparent fix, header icon dark-safe. CLAUDE.md: dispatch model architecture added (orchestrator coordinates, backend→Opus, frontend style→Gemini via /agy, frontend flow→Opus, review split by domain). dispatch-log.jsonl initialized (4 entries). tsc clean, 70/70 e2e + 1 skipped.
	E2E reliability fixes (phase gate): 5 root causes fixed. (1) UUID collision: journeyGroup ...000015 collided with noWindowGroup → changed to ...0001e0. (2) CSS class locator breakage: 5R.5 changed balance from text-7xl to text-5xl, clashing with stepper → added data-testid="card-balance"/"purchase-qty" + updated 4 spec files. (3) Strict-mode on 繳費對帳: student-journey creates additional courseFee orders → filter on unique remittance last5 (54321/12345) instead of $800/CardContent class. (4) Cross-test card_balance contamination: enroll-gating modifies card10.used but refund-count/register-modify expected seed values → added resetCardSeedState() restoring order used counts before each test. (5) Shadcn 2.x class→data-slot: card-purchase regression used [class*="CardContent"] (Shadcn v1) which returns 0 → [data-slot="card"] + last5 filter. Full suite: 78 passed, 1 skipped (date-dep), 0 failed.
	D1/D2/D3 HOTFIX (order-enrollment lifecycle gaps): 3 CRITICAL money-path bugs fixed in actions.ts. D1: confirmOrder card_purchase branch now activates linked pending_payment enrollments (query→per-enrollment FIFO deduct future sessions→status flip to enrolled, try/catch per-enrollment with partial-success tracking). D2: cancelOrder+rejectOrder enrollment cascade extended from course_fee-only to card_purchase|course_fee. D3: cancelEnrollment blocks partial cancel when order has sibling active enrollments (redirects to cancel-order/modify-registration). Dead code cleanup: removed 30-line paymentGroups IIFE in approvals-tabs-client.tsx (computed but never rendered). 4 adversarial e2e tests in order-lifecycle.spec.ts (D1 confirm→activate+deduct, D2a cancel→cascade, D2b reject→cascade+reason, D3 sibling-block) with per-test fixture isolation. Mutation tests 3/3 RED (all guards load-bearing). Review round: 8 findings fixed (D1 try/catch, D2a cancelOrder cascade was missing card_purchase + MUTATION-B residue, 6 broken e2e tests from UI renames, ORDER_STATUS_LABELS.confirmed→'已確認', lint let→const + unused imports, Suspense fallback, refund-count tab fix). Final gate: Opus 4.6 reviewer APPROVED — 81 pass / 1 fail (refund-count tab fix applied post-review) / 2 skip. Tree integrity SHA-256 verified. WORKFLOW ENFORCEMENT LAYER: 5 hooks in .claude/hooks/ (dispatch-guard blocks weak-model money review, commit-gate blocks commit without ledger, session-context auto-injects state post-compaction, stop-gate blocks session end without ledger). Live-fire tested: haiku dispatch blocked in real-time.
	5R-UX-supplemental (Task #16): 10 files changed. (a) Course details: enrollment query includes teacher field, display "teacher courseName" in my_cards + approvals. (b) Rename: 堂卡與繳費→我的購買 (dashboard title+subtitle, my-cards header). (c) BankInfo relocation: removed from payments tab listing, added to purchase dialog step 2 + wizard done step amber warning. (d) Visual overhaul (Gemini Flash /agy): premium balance card restored (gradient, shadow-2xl, CreditCard watermark, text-7xl, orange accents), purchase dialog orange-600 theme, header icon. (e) Wizard done step: amber warning block for pending_payment (報名尚未完成+bankInfo+匯款指引), button→"前往我的購買". (f) CSS fix: hidden sm:flex conflict with standalone flex on header icon. Reviewer (Opus 4.6): tsc clean, 12/12 scoped pass; full suite hung (killed after 7h). Reviewer finding: hidden sm:flex CSS conflict → fixed. Orchestrator phase gate: 82 pass / 2 skip / 0 fail. CLAUDE.md: review dispatch updated (Fable default, Opus fallback). Hooks: sdd-dispatch-guard simplified (haiku-only block), sdd-ui-visual-guard added (4+ className change warning).
	Payment deadline spec (Task #17): Design spec written at docs/superpowers/specs/2026-07-07-payment-deadline-design.md. Concert ticket reservation model: per-group payment_deadline_days + per-enrollment payment_deadline_at. Lazy check (page load) + cron (hourly Edge Function). Decision log #34 added. Self-reviewed: 3 gaps fixed (entry points, validation location, timing). Awaiting user review before implementation plan.
	5R-UX-round2 (Task #19): Tab alignment + order card course detail + visual polish. (a) Tab styling: removed max-w-lg constraint, TabsList→sm:flex sm:w-auto, TabsTrigger→px-3 sm:px-4 to match approvals pattern. (b) Data restructure: courseNames:string[] → courseDetails:{name,teacher}[] across 4 files (my_cards/page, my-cards-client, approvals/page, approvals-tabs-client). (c) Order card visual (/agy Gemini Flash): structured course breakdown cards with BookOpen/User/Calendar icons, orange theme accents, "總額" label, course_fee orders show individual course items with teacher + name, card_purchase shows quantity badge + price breakdown, filter chips orange active state, rejected message styled box, action buttons orange-themed. (d) Dead code cleanup: removed unused `summary` variable. Reviewer (Opus 4.6): APPROVED — 81 pass / 2 skip / 1 fail (pre-existing capacity flaky, unrelated). 1 LOW finding (dead code) fixed.
	5R-UX-round2-testfix (Task #19 cont.): Admin course_fee card "繳費合計" total line added (was missing when courseDetails.length > 0, breaking test locators that filter by '繳費'). Test locators updated: getByText('800').first() in admin-journey.spec.ts and review-center-coursefee.spec.ts to handle strict-mode violation from per-course + total displaying same amount. Width wrapper (max-w-lg mx-auto) confirmed restored. All 6 affected tests pass (admin-journey 3/3, review-center-coursefee 3/3, student-journey 5/5).
	S0 RLS Security Hotfix (2026-07-10): Closed 3 pre-existing security holes discovered during design v2 adversarial review. (1) profiles UPDATE policy allowed any authenticated user to self-escalate to admin role. (2) enrollments ALL policy allowed direct writes bypassing all server guards (capacity, windows, pricing, identity). (3) orders INSERT+UPDATE policies allowed pending order amount tampering. FIX: migrated 4 write paths (enrollInCourse, cancelEnrollment, updateMemberProfile, createCardOrder) from user-scoped `supabase` to `adminClient` (service-role). Deleted dead code: updateUserRole in user-actions.ts (the actual escalation vector) + DevRoleToggle component (sole consumer, never rendered). Migration 016_rls_hardening.sql: DROP all write policies on profiles/enrollments/orders (keep SELECT + profiles INSERT for auth signup), re-create admin SELECT on orders, REVOKE EXECUTE on enroll_atomic+void_group_submission from anon+authenticated, REVOKE is_admin from anon. Applied to DEV. Reviewer (Opus 4.6): PASS — 82/2skip/0fail, tsc clean, DB mutation check verified, dead code grep clean. 1 MINOR: course_leaders writes still user-client (admin-gated, out of scope). PROD pending user confirmation. CLAUDE.md: next migration updated to 017.
